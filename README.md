# Burger-Recipe
Simple Ionic App for listing some of the best In-N-Out Burgers

## Install
git clone the repo
cd into project
run npm install

## Development server

Run `ionic serve --lab` for a dev server. 


